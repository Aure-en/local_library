extends layout

block content
  h1= title

  ul
    - book_list.sort((a, b) => a.title.toUpperCase().localeCompare(b.title.toUpperCase()));

    each book in book_list
      li
        a(href=book.url) #{book.title}
        |  (#{book.author.name})

    else
      li There are no books.